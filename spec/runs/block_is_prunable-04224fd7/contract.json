{
  "preconditions": [
    "true"
  ],
  "postconditions": [
    "result == (retain > 0 && head > retain && block <= head - retain)"
  ],
  "acceptance_criteria": [
    "A block is prunable only when retain is positive, head is beyond the retained count, and the block is at or before head - retain."
  ],
  "test_evidence": [
    {
      "scenario_id": "block_is_prunable-nominal",
      "command": "cargo test -p verified-core prune && cargo test -p ic-evm-core prune && cargo test -p ic-evm-gateway pruned && cargo test -p ic-evm-gateway get_receipt_returns",
      "test_name": "verified-core prune safety plus evm-core prune and gateway pruned/retained query adapter tests",
      "recorded_at": "2026-05-16T03:08:48Z"
    }
  ],
  "risk_level": "medium"
}
