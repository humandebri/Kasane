{
  "preconditions": [
    "true"
  ],
  "postconditions": [
    "result == (!input.tx_store && !input.receipt && !input.tx_index && !input.internal_traces && !input.tx_loc && !input.seen_tx)"
  ],
  "acceptance_criteria": [
    "A pruned transaction is cleanup-complete only when tx storage, receipt, tx index, internal traces, location, and seen-tx observations are all absent."
  ],
  "test_evidence": [
    {
      "scenario_id": "prune_tx_cleanup_complete-nominal",
      "command": "cargo test -p verified-core prune && cargo test -p ic-evm-core prune && cargo test -p ic-evm-gateway pruned && cargo test -p ic-evm-gateway get_receipt_returns",
      "test_name": "verified-core prune safety plus evm-core prune and gateway pruned/retained query adapter tests",
      "recorded_at": "2026-05-16T03:09:03Z"
    }
  ],
  "risk_level": "medium"
}
