{
  "preconditions": [
    "true"
  ],
  "postconditions": [
    "result == (version == COMPACT_FORMAT_VERSION && kind == ICP_QUERY_KIND_QUERY && target_len >= 1 && target_len <= MAX_PRINCIPAL_LEN && target_present == 1 && method_len >= 1 && method_len <= MAX_QUERY_METHOD_LEN && method_present == 1 && method_utf8 == 1 && arg_present == 1 && consumed_exact == 1)"
  ],
  "acceptance_criteria": [
    "Accept only compact query inputs with version 1, query kind, bounded target and method, UTF-8 method bytes, present argument bytes, and no trailing data."
  ],
  "test_evidence": [
    {
      "scenario_id": "compact_icp_query_input_safe_raw-nominal",
      "command": "cargo test -p ic-evm-core icp_query_precompile",
      "test_name": "icp_query_precompile parser/model PBT and compact parser boundary tests",
      "recorded_at": "2026-05-22T07:18:32Z"
    }
  ],
  "risk_level": "medium"
}
