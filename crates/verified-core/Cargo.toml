[package]
name = "verified-core"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
name = "verified_core"
crate-type = ["rlib"]
doctest = false

[dependencies]

[dev-dependencies]
proptest = "1.6"

[lints.rust]
unexpected_cfgs = { level = "warn", check-cfg = ["cfg(verus_keep_ghost)"] }
