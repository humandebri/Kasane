// どこで: wrapper dashboard page / 何を: 最小ダッシュボードを表示 / なぜ: submitからstatus照会までを一画面で完結させるため

import { WrapperDashboard } from "@/components/dashboard";

export default function Page() {
  return <WrapperDashboard />;
}
